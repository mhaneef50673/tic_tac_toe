import React from "react";

/**
 * Renders when any url doesnt match the route
 */
const NotFound = (props) => {
  return (
    <React.Fragment>
      <h1>Sorry, Page does not exists</h1>
    </React.Fragment>
  );
};

export default NotFound;
